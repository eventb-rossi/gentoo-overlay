masters = gentoo
thin-manifests = true
sign-manifests = false
# ::gentoo's list plus the Portage-specific network-sandbox value,
# used by packages that must resolve build dependencies online.
restrict-allowed = binchecks bindist fetch installsources mirror network-sandbox preserve-libs splitdebug strip test userpriv
